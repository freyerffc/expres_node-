name: Prueba num 1

on:
  push:
    branches: 
      - main 

jobs:
  decir-hola:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write 

    steps:
      - name: Mostrar mensaje
        run: echo "Hola mundo"
      - name: Enviar peticion
        run: |
          curl -X POST 'https://webhook.site/892f2749-aba3-44c8-af63-04640edf7a85' \
          -H 'content-type: application/json' \
          -d '{"nombre": "Freddy"}'
      - name: Checkout repository
        run: actions/checkout@v4
      - name: Logearnos en GHCR
        run: echo "$(( secrets.TOKEN ))" | docker login ghcr.io -u freyerffc --password-46L@54z32Y54
      - name: Crear imagen
        run: docker build . -t ghcr.io/freyerffc/express/-image
      - name: Desplegar
        run: docker push -t ghcr.io/freyerffc/express-image


    

